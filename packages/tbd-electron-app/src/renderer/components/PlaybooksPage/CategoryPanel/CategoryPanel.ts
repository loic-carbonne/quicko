import React, { FunctionComponent } from 'react';

type CategoryPanelProps = {
};

const CategoryPanel: FunctionComponent<CategoryPanelProps> = ({ value = '', onChange }) => {
  return (
    <div>
    <div/>
  );
};

export default CategoryPanel;
